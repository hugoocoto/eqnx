#ifndef CONFIG
#define CONFIG

// try to find a font by path or by name. Font's size is fontsize
const char *const fontpath = 0;             // 0 or absolute font path. 0 means "use fontname"
const char *const fontname = "Iosevka NFM"; // 0 or font name. 0 means "use default"
const int fontsize = 48;                    // font size (aka font height)

#endif
