-- try to find a font by path (fontpath) or by name (fontname). Font's size is fontsize
Font = {
    -- path = "/usr/share/fonts/TTF/IosevkaNerdFontMono-Medium.ttf" -- Absolute font path.
    name = "Iosevka NFM", -- Font name.
    size = 36,            -- Font size (font height in px).
}
